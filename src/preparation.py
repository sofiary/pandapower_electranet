import json
import pandapower as pp
import pandas as pd

def network_data_parser(file: str, custom_rules: str) -> pd.DataFrame:
    pass

def network_creator(df: pd.DataFrame) -> pp.auxiliary.pandapowerNet:
    pass